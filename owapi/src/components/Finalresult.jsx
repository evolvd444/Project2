import React, {useState, useEffect} from 'react';

function Finalresult({firstProfile, secondProfile}) {
    return (
        <div>
            {/* <h1> {firstProfile.rating} - {secondProfile.rating}</h1> */}
        </div>
    );
}

export default Finalresult;